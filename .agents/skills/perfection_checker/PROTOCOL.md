# 🚀 ANTIGRAVITY PERFECTION PROTOCOL V1.0

This protocol is a **Self-Upgrade** for my logical reasoning and internal verification cycle. I will execute these steps as a "Local Subagent" process after every masterpiece restoration.

## 🛡️ STEP 1: THE SYNTAX SHIELD
- **Action**: Run `audit_syntax.ps1`.
- **Constraint**: If `tsc` reports any error, I am FORBIDDEN from reporting completion. I must surgically fix the edit.

## 🏛️ STEP 2: THE GEOMETRIC AUDIT
- **Action**: Run `audit_geometry.py`.
- **Constraint**: Every `gen_` function MUST recalculate `nTheta` (steps) dynamically using the Arc-Length formula: `Math.ceil((2 * PI * R) / 2.1)`.
- **Constraint**: Hardcoded `steps < 16` for `radius > 10` is an automatic failure.

## 🛡️ STEP 3: THE HEALTHY SCALE GOVERNOR
- **Mandatory**: Every generator must return `applyGovernor(pts, 1200)`.
- **Performance**: Never exceed 1200 objects. Quality is achieved through TILING, not density stacking.

## 🔄 EXECUTION LOOP
1. **Develop** (Rewrite generator).
2. **Audit** (Invoke Subagents).
3. **Verify** (Browser Preview).
4. **Signal** (Report Success ONLY if Audits pass).
